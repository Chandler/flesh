<div class="container">
  <div class="jumbotron">
    <h3>Welcome to <span class="logo">flesh</span> </h3>
    <div class="alert alert-warning"> 
      {{render sms_instructions}}
    </div>
    {{#if session.isAuthenticated}}
    {{else}}
      <p>
        To get started
        {{#link-to 'login'}} Login {{/link-to}} or
        {{#link-to 'users.create'}} Create a new account {{/link-to}}
      </p>
    {{/if}}
  </div>
  <div>
    {{partial 'discovery'}}
  </div>
</div>
